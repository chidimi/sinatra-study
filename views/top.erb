<!DOCTYPE html>
<html lang="ja">
<head>
  <mata charset="utf-8">
  <title>top</title>
</head>
<body>
  <h1>メモアプリ</h1>
  <a href="newmemo">新規追加</a>
  <ul>
    <% @memos.each do |memo| %>
    <li>
        <a href="memodetail/<%= h memo['id'] %>"><%= h memo['title'] %></a>
    </li>
    <% end %>
  </ul>
</body>
</html>
